@model Vidly.Models.Customer

<h1>@Model.Name</h1>
<ul>
    <li>@Model.MembershipType.Name</li>
    @if (Model.Birthday != null)
    {
        <li>@Model.Birthday.Value.ToString("dd/MM/yyyy")</li>
    }
</ul>

